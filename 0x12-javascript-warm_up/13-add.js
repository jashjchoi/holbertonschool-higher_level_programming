#!/usr/bin/node
/*
  prints the addition of 2 integers
  Function must be visible from outside (13-main.js)
  Name of the function must be add
*/

exports.add = function (a, b) {
  return (a + b);
};
